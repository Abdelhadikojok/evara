import { InfoDirectiveDirective } from './info-directive.directive';

describe('InfoDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new InfoDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
